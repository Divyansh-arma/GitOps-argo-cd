apiVersion: v1
kind: Service
metadata:
  name: {{ include "py-app-helm.fullname" . }}
  labels:
    {{- include "py-app-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "py-app-helm.selectorLabels" . | nindent 4 }}
